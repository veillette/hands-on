# MyST GitHub Pages Deployment Workflow
# Auto-generated with enhanced configuration for optimal performance and security

name: MyST GitHub Pages Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:  # Allows manual triggering from the Actions tab

env:
  BASE_URL: /${{ github.event.repository.name }}
  NODE_VERSION: '20.x'

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: 'pages'
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 15  # Prevent runaway builds
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1  # Shallow clone for faster checkout
      
      - name: Setup Pages
        uses: actions/configure-pages@v4  # Updated to v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'  # Cache npm dependencies
      
      - name: Install MyST Markdown
        run: npm install -g mystmd
      
      - name: Cache build artifacts
        uses: actions/cache@v3
        with:
          path: ./_build
          key: myst-build-${{ github.sha }}
          restore-keys: |
            myst-build-
      
      - name: Build HTML Assets
        run: |
          echo "Building site with BASE_URL: $BASE_URL"
          myst build --html
        env:
          NODE_OPTIONS: "--max-old-space-size=4096"  # Prevent memory issues
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3  # Using v3 which is currently available
        with:
          path: './_build/html'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
      
      - name: Deployment summary
        run: |
          echo "🚀 Successfully deployed to ${{ steps.deployment.outputs.page_url }}"
          echo "Build completed at $(date)"
